# Copy this file to .env and fill in values.
#   Windows (PowerShell): Copy-Item .env.example .env
#   macOS/Linux:         cp .env.example .env

# -----------------------------
# Azure OpenAI (required)
# -----------------------------
# IMPORTANT: The endpoint should NOT include '/openai'.
# Example:
#   https://<your-resource-name>.openai.azure.com/
AZURE_OPENAI_ENDPOINT=

# Azure Portal -> Keys and Endpoint
AZURE_OPENAI_API_KEY=

# Matches the default used by the app if not provided.
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# Your Azure OpenAI deployment name (the deployment in Azure AI Foundry/Studio)
AZURE_OPENAI_DEPLOYMENT_NAME=

# Optional: API mode used by the OpenAI Python SDK integration.
# - chat_completions: default, compatible with many deployments
# - responses:        required by some newer deployments (e.g., GPT-5)
AZURE_OPENAI_API_MODE=chat_completions

# -----------------------------
# Cloudflare Tunnel (optional)
# -----------------------------
# If set, the cloudflared service can create a public tunnel.
CLOUDFLARED_TOKEN=

# -----------------------------
# Build metadata (optional)
# -----------------------------
# Used for display/debug; Docker build arg can set this.
APP_VERSION=dev
